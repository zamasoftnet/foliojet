# Composite-build project dependencies must be supplied to javac as JARs.
# On Windows-hosted DrvFs, Gradle can otherwise invalidate an included
# project's classes directory between compilation and classpath snapshotting.
systemProp.org.gradle.java.compile-classpath-packaging=true

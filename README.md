# FolioJet

HTML/CSS などのページング処理を担う公開ライブラリです。

Gradle ベースのマルチプロジェクト構成です。ビルドは `./gradlew build` を使います。CTI Java 本体は外部の `F:/dev/zamasoftnet-public/cti.java` を composite build で参照します。

Maven の `pom.xml` と Eclipse のプロジェクトメタデータには依存しません。

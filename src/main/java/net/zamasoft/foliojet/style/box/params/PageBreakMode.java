package net.zamasoft.foliojet.style.box.params;

/**
 * 改ページ・改段の種別(page-break-before/after/inside)です。
 *
 * @author MIYABE Tatsuhiko
 */
public enum PageBreakMode {
	AUTO, AVOID, PAGE, VERSO, RECTO, COLUMN, IF_VERSO, IF_RECTO
}
